Flask>=3.0.0
python-dotenv>=1.0.1
requests>=2.31.0
PyJWT>=2.8.0
redis>=5.0.1
gunicorn>=21.2.0
Flask-CORS>=4.0.0
Flask-SocketIO>=5.3.6
python-socketio>=5.10.0
python-engineio>=4.8.0
websockets>=10.4
websocket-client
eventlet

# Required by run_gevent.py, which is the documented way to start the
# proxy (main.py has no WebSocket support). Without these a fresh
# `pip install -r requirements.txt` produces an install that cannot start.
gevent>=24.0.0
gevent-websocket>=0.10.1

# Runtime configuration store: routes and backends live in MongoDB so they
# survive restarts and can be managed from the admin panel. The proxy falls
# back to .env when the database is unreachable, so this is not fatal.
pymongo>=4.6
